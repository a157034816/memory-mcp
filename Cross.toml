# 用于本仓库的 `cross` 构建配置。
#
# 背景：部分旧版 cross 镜像（例如 `:0.2.5`）在较新的 Rust toolchain 下可能出现
# “GLIBC_2.xx not found” 的 build-script 运行失败。
# 这里将 musl 目标固定到 `:latest`，以避免镜像基底过旧导致的兼容性问题。

[target.x86_64-unknown-linux-musl]
image = "ghcr.io/cross-rs/x86_64-unknown-linux-musl:latest"

[target.aarch64-unknown-linux-musl]
image = "ghcr.io/cross-rs/aarch64-unknown-linux-musl:latest"

